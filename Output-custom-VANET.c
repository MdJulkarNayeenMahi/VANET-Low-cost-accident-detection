#include <stdio.h>
int main(void )
{


   int c;

   printf( "Server Input:");
   c = getchar( );

   printf( "\nVehicle:436,Showing:11,Accident happened");
   putchar( c );

   int d;

   printf("Server Output:");
   d = getchar( );

   printf("\nVehicle:436,is in danger and proceed to ERS");
   putchar( d );

int e;

   printf("Server Input:");
   e = getchar( );

   printf("\nVehicle:211,Showing:00,Vehicle is running normally");
   putchar( e );

   int f;

   printf("Server Output:");
   f = getchar( );

   printf("\nVehicle:211,is not in danger");
   putchar( f );

   int g;

   printf("Server Input:");
   g = getchar( );

   printf("\nVehicle:190,Showing:00,Vehicle is running normally");
   putchar( g );

   int h;

   printf("Server Output:");
   h = getchar( );

   printf("\nVehicle:190,is not in danger");
   putchar( h );

   int i;

   printf("Server Input:");
   i = getchar( );

   printf("\nVehicle:250,Showing:11,Accident happened");
   putchar( i );

   int j;

   printf("Server Output:");
   j = getchar( );

   printf("\nVehicle:250,is in danger and proceed to ERS");
   putchar( j );

   int k;

   printf("Server Input:");
   k = getchar( );

   printf("\nVehicle:349,Showing:00,Vehicle is running normally");
   putchar( k );

   int l;

   printf("Server Output:");
   l = getchar( );

   printf("\nVehicle:349,is not in danger");
   putchar( l );

   int m;

   printf("Server Input:");
   m = getchar( );

   printf("\nVehicle:402,Showing:11,Accident happened");
   putchar( m );

   int n;

   printf("Server Output:");
   n = getchar( );

   printf("\nVehicle:402,is in danger and proceed to ERS");
   putchar( n );


   return 0;
}
